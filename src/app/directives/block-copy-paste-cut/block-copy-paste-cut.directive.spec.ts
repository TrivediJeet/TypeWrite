import { BlockCopyPasteCutDirective } from './block-copy-paste-cut.directive';

describe('BlockCopyPasteCutDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockCopyPasteCutDirective();
    expect(directive).toBeTruthy();
  });
});
